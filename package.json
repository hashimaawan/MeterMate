{
  "name": "metermate",
  "version": "1.0.0",
  "private": true,
  "description": "MeterMate — a two-sided billing concierge: Maxio billing narrated in a private Slack channel per consultant↔client transaction.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace server",
    "typecheck": "npm run typecheck --workspace server",
    "test": "npm run test --workspace server",
    "seed": "npm run seed --workspace server"
  },
  "engines": {
    "node": ">=20"
  }
}
